# Your Anthropic API key for Claude
ANTHROPIC_API_KEY=your_api_key_here

# Optional: Path to service account credentials (if using service account instead of OAuth)
GOOGLE_CREDENTIALS_PATH=path/to/your/service_account_credentials.json
